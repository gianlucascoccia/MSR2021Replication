i am work on a note take electron app that use markdown. currently, i'm work on insert imag into note (use the markdown syntax).when insert an image, my main process copi the imag into the note directory, then return a .is there a way i can configur electron to allow these local imag urls?
