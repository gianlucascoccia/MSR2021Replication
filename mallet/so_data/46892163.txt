i'm work with an exist electron project (convert web app to desktop app), which has a task that is to export content on screen to pdf/png/jpg.here is the situation:th desktop app is pure client-sid code, it doesn't connect to ani api or server (just in case you suggest a solut use nodej server-sid code)i got the dataurl from canva object alreadi (it a base64 string of the file)how can i save that dataurl into a file (pdf/png/jpg)?here are some way that i tried:th good old window.loc = dataurl (noth happens)cr a form insid the div, action = dataurl, then submit the formboth way are not working!thank you veri much
