i'v got a littl problem on a electron application. i have some kind of js code where i fill an array with objects. want to use the data somewher else. console.log() show me the array with void squar brackets. if i open it the date is there. but if i want to get access like myarr[0].id the develop consol onli push undefined.aft that piec of code i creat a new array and fill it with object as a refer to that behavior, which i describ above. but on this array i get ordinari access and console.log() show this array up in round bracket with the length in it.can someon pls. explain me the behavior or how i get access to the object in the first array?th problem array:first array ist the problem one and the second the refer one
