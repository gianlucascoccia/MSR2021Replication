current i need to get command line argument from an electron application. so if you start the applic with command line arguments, i need to use the argument within the render process (a webview to be specific.)thi work fine when open the applic for the first time (use onload), but i'm have issu get this to work when the applic is alreadi run and the user tri to reopen the app from cmd with args.currently, i'm using:to prevent a second window from opening. however, i need the commandlin argument and i need to send them to the render process.i there a way to do this?
