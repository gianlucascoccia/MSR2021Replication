i'm build an electron applic with vue.j as the client side framework. when i tri to run a node script i get uncaught (in promise) typeerror: self._form.on is not a functioni'm not sure if this is due to it be run on the browser via electron but it is work fine when i run the script via node in the console. is it even possibl to run this node script client side? i'm run the node file via one of the frontend vue.j compon by requir the file. let rafflescript = require('../raffles/index.js'); node script:
