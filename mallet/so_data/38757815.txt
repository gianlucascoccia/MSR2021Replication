i am use electron. i have written a program that recurs read the file of a directory. now i want that file with a certain extnam (file extension) are not shown. in order to do that i need the node.j method path.extname(path) which return the extname.mi problem is that the path.extname() method doe not work at that place in the code i need it.outsid of my function scan_directory_to_html() it work and return .jpg but insid of the function i get the error insid the function as well but it then say that the identifi path has alreadi been declared.how can i use the path.extname() method insid of the function?
