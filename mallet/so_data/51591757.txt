i'm current work on an electron app on the client side, and an apach server for the backend.thi mean that the app and the server are not on the same domain and the php file are call via jsonp (this is on a privat network).mi problem is that i cannot set $_session and $_cooki as the "calls" are made with jsonp and that the server is make a new session for each one : when i output the session_id() it is differ everytime.to be clear this is what i mean as json call (done in electron) : are there other solut ? which one would be better ?ani feedback is appreci !
