i have a run electron app ( base on angular ) which should so some work befor the app is closed. insid the app i have some listen which will react on close / beforeunload and pagehid : that work fine if the user close the app the "normal" way but that code isn't execut wenn the user shutdown the system ( target: window 10 ). i tri to implement it with the session-end-ev but that do not seem to work either. > the session-end event is called, but it seem that the shutdown callback isn't called.i there ani way to handl a system reboot / shutdown insid the app?main.j
