i'm use nightmare.j to scrape webpag content. after authent the nightmar instance, i loop through multipl page (that requir a login) and then call the run method that execut all the page loads. from each page i want to grab specif content so i call the evalu function which ensur we'r execut insid the browser scope. whatev is return from the evalu function becom the argument in the run methodbut i'm tri to run the evalu function multipl time (onc for each page). but the evalu function can onli return output to the run method once. i tri push content to a global variabl (in the main scope) but can't access it from insid the browser scope. can anyon advis on how to have multipl evalu method run on one nightmar instanc and extract inform from each?
