environment: electron 1.6.11 (which includ nodejs) and bootstrap 3.3.x for the uidesir results:i would like to:start a indetermin progress bar when the run button is clickedrun a process (a java program) -- which run successfullyremov the indetermin progress bar when the process (java program) returnswhat i get in the follow code is: the process (java program) startsth process (java program) endsthen the indetermin progress bar appear (i cannot remov it when tri -- see comment code)possibly, i am not synchron my process correctly. i thought the nodej code
