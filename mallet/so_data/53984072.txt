here is the "for" loop to run query(sqlite3 database) for each "id" in an array.&#xd;&#xd;&#xd;&#xd;&#xd;th problem is that the for loop continu befor the queri is finished. is there a way to check if the queri has finished?
