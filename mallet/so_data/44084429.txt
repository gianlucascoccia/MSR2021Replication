we'r upgrad a old window app which use .net framework to a new app which doe not (an electron app). we need to migrat each user set from the old app into the new version. the set are store in a user.config file use the system.configur packag in .net framework. how can we reliabl get the path of the user.config file creat by the prior version of the app from our new app? the path has a long hash or guid in it (and i assum the path and hash vari in each instal and version of windows).thi method work from .net, but our new app doesn't have .net:
