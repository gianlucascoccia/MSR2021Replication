i'm tri to launch an .ex elsewher on my window filesystem. but that just error with the same code, it like the try/catch block error is be thrown befor the callback from the spawn function is called.interest i'v tri call a .ex in the same directori and it doe work...
