i'm use to open the url in the default browser of the user.mi problem is that the browser window isn't "focused". - i stay in the wrong workspace.on :https://electronjs.org/docs/api/shell#shellopenexternalurl-optionsther is an option "activate" appar onli for macos.how can i do that on window / linux ?
