i'm tri to make an electron app with an input, which valu will be use for set the app progress bar via i except the app progress bar to be set to the valu i'm put in the input field when the event on it happens, via the render process and ipc.accord to devtools, it look like the render process function use for the event listen run onc at load, and the event listen isn't appli to the input field.her what show in the console, doesn't depend on if someth was type in the input or not :https://i.imgur.com/poxxoy5.png .and in the event listen tab for the input element, no event listen show from the render script.
