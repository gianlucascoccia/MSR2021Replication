when i tri to run a mobilenet (use product built script) use tensorflowj insid an electron app i get the follow error. however, the same code run in chrome and firefox without ani issuesher is what my package.json look like:
